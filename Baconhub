-- // BACON HUB: RAW (PUBLIC SYNC) //
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local Config = { 
    Color = Color3.fromRGB(255, 0, 80), 
    Bg = Color3.fromRGB(15, 15, 15), 
    Accent = Color3.fromRGB(30, 30, 30),
    SoundId = "rbxassetid://1837874720", -- Public Loud Clapping
    Pitch = 0.8,
    Vol = 5
}

-- // UI CONSTRUCTION //
local Screen = Instance.new("ScreenGui", LocalPlayer.PlayerGui); Screen.Name = "BaconRawFinal"; Screen.ResetOnSpawn = false

-- Main Frame (Modern Dark)
local Main = Instance.new("Frame", Screen); Main.Size = UDim2.new(0, 180, 0, 260); Main.Position = UDim2.new(0.5, -90, 0.5, -130)
Main.BackgroundColor3 = Config.Bg; Main.BorderSizePixel = 0; Instance.new("UICorner", Main)
local mainStroke = Instance.new("UIStroke", Main); mainStroke.Color = Config.Color; mainStroke.Thickness = 2

-- Toggle Button
local SideBox = Instance.new("TextButton", Screen); SideBox.Size = UDim2.new(0, 40, 0, 40); SideBox.Position = UDim2.new(0, 15, 0.5, -20)
SideBox.BackgroundColor3 = Config.Bg; SideBox.Text = "B"; SideBox.TextColor3 = Config.Color; SideBox.Font = Enum.Font.GothamBold; SideBox.TextSize = 20
Instance.new("UICorner", SideBox); Instance.new("UIStroke", SideBox).Color = Config.Color

local Title = Instance.new("TextLabel", Main); Title.Size = UDim2.new(1, 0, 0, 40); Title.Text = "BACON HUB"
Title.TextColor3 = Config.Color; Title.Font = Enum.Font.GothamBold; Title.TextSize = 15; Title.BackgroundTransparency = 1

local List = Instance.new("ScrollingFrame", Main); List.Size = UDim2.new(1, -20, 1, -55); List.Position = UDim2.new(0, 10, 0, 45)
List.BackgroundTransparency = 1; List.ScrollBarThickness = 0; Instance.new("UIListLayout", List).Padding = UDim.new(0, 8)

-- // DRAGGING //
local function Drag(gui)
    local dragging, dragStart, startPos
    gui.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; dragStart = i.Position; startPos = gui.Position end end)
    UIS.InputChanged:Connect(function(i) if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = i.Position - dragStart
        gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end end)
    UIS.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)
end
Drag(Main); Drag(SideBox)

-- // FUNCTIONS //
local function GetNearest()
    local target, dist = nil, 200
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local d = (LocalPlayer.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
            if d < dist then dist = d; target = p.Character end
        end
    end
    return target
end

local function AddBtn(txt, callback)
    local b = Instance.new("TextButton", List); b.Size = UDim2.new(1, 0, 0, 35); b.BackgroundColor3 = Config.Accent
    b.Text = txt; b.TextColor3 = Color3.new(1,1,1); b.Font = Enum.Font.GothamBold; b.TextSize = 11; Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(callback); return b
end

-- // FEATURES //
AddBtn("ATTACK (INSTANT)", function()
    local n = GetNearest()
    if n then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local old = hrp.CFrame
        hrp.CFrame = n.HumanoidRootPart.CFrame
        hrp.Velocity = Vector3.new(99999, 99999, 99999)
        task.wait(0.25)
        hrp.Velocity = Vector3.zero; hrp.CFrame = old
    end
end)

AddBtn("KICK (3S)", function()
    local n = GetNearest()
    if n then
        local hrp = LocalPlayer.Character.HumanoidRootPart
        local old = hrp.CFrame
        local s = tick()
        while tick() - s < 3 do
            hrp.CFrame = n.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(tick()*5000), 0)
            hrp.Velocity = Vector3.new(99999, 99999, 99999)
            task.wait()
        end
        hrp.Velocity = Vector3.zero; hrp.CFrame = old
    end
end)

local active = false
local MySnd = Instance.new("Sound", game:GetService("SoundService"))
MySnd.SoundId = Config.SoundId; MySnd.Volume = Config.Vol; MySnd.PlaybackSpeed = Config.Pitch; MySnd.Looped = true

local goonBtn = AddBtn("GOONING (OFF)", function() end)
goonBtn.MouseButton1Click:Connect(function()
    active = not active
    goonBtn.Text = active and "GOONING (ON)" or "GOONING (OFF)"
    goonBtn.BackgroundColor3 = active and Config.Color or Config.Accent
    
    if active then
        MySnd:Play()
        -- Warning
        local w = Instance.new("TextLabel", Screen); w.Size = UDim2.new(0, 260, 0, 40); w.Position = UDim2.new(0.5, -130, 0.2, 0)
        w.Text = "WARNING: LOUD SOUNDS - LOWER VOLUME"; w.BackgroundColor3 = Color3.new(0,0,0)
        w.TextColor3 = Config.Color; w.Font = Enum.Font.GothamBold; Instance.new("UIStroke", w).Color = Config.Color
        task.delay(4, function() w:Destroy() end)
    else
        MySnd:Stop()
    end
    
    task.spawn(function()
        while active do
            local n = GetNearest()
            if n then LocalPlayer.Character.HumanoidRootPart.CFrame = n.HumanoidRootPart.CFrame * CFrame.new(0,0, math.sin(tick()*20)*3) end
            task.wait()
        end
    end)
end)

SideBox.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)
